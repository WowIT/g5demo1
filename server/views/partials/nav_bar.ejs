
<!-- set active item on navigation bar based on the current page -->
<div id="mySidenav" class="sidenav">
    <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
    <a  href="/home"><i class="fas fa-home"></i> Home</a>
    <a  href="/incident-list"><i class="fas fa-address-book"></i> Incident List</a>
    <a  href="/incident-list/dashboard"><i class="fas fa-address-book"></i> Dashboard</a>
    <% if(typeof displayName == 'string' && displayName){ %>
        <a href="/logout"><i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
    <% } else { %>
        <a href="/login" class="nav-link"><i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
    <% } %>
    </div>

<!-- Main navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div>
        <span style="font-size:26px;cursor:pointer;color:white" onclick="openNav()">&#9776; Menu</span>
    </div>
    <div class="brand-logo ml-auto">
      <!-- Condition Welcome Message -->
      <% if(typeof displayName == 'string' && displayName){ %>
        <span class="navbar-text hello_msg">Welcome, <%= displayName %></span>
      <% } %>
      <!-- Logo Img -->
      <a href="/"><img src="/assets/images/logo.png" alt="logo" style="height: 55px; width:65px; padding: 0 auto;"></a>
    </div>
</nav>
